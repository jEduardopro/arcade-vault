# Build output and dependencies
.next/
out/
build/
node_modules/
next-env.d.ts
*.tsbuildinfo

# Lockfiles
package-lock.json

# app/globals.css is a byte-for-byte port of
# references/templates/home-about/styles.css (see CLAUDE.md). Reformatting it
# would break that property, so Prettier must leave it alone.
app/globals.css

# Reference templates are the design source of truth, kept verbatim.
references/

# Regenerated by `next dev`
# (node_modules/next/dist/server/lib/generate-agent-files.js).
AGENTS.md

# Playwright MCP scratch output
.playwright-mcp/
