# Contact form (SPEC 03). Copy this file to .env.local and fill in real values.
# None of these are prefixed with NEXT_PUBLIC_: they are read only inside the
# server action at app/actions/contact.ts and never reach the browser.

# Resend API key. Without it, `next dev` falls back to console mode: the message
# is printed to the server log and the form reports success. In production a
# missing key makes the form return an error instead.
RESEND_API_KEY=re_xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx

# Mailbox that receives the contact messages.
CONTACT_TO_EMAIL=team@example.com

# Sender. The domain has to be verified in Resend or every send fails.
CONTACT_FROM_EMAIL=Arcade Vault <hello@example.com>
